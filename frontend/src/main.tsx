import React from "react" import ReactDOM from "react-dom/client" import App from "./App" // TRPC CLIENT — FIXED PATH import { trpc } from "./lib/trpc" // OPTIONAL: React Query (if your project uses it) import { QueryClient, QueryClientProvider } from "@tanstack/react-query" import { httpBatchLink } from "@trpc/client" // Create TRPC + React Query client const queryClient = new QueryClient() const trpcClient = trpc.createClient({ links: [ httpBatchLink({ url: "/api/trpc", }), ], }) ReactDOM.createRoot(document.getElementById("root")!).render( <React.StrictMode> <trpc.Provider client={trpcClient} queryClient={queryClient}> <QueryClientProvider client={queryClient}> <App /> </QueryClientProvider> </trpc.Provider> </React.StrictMode> )